# Changelog

## Unreleased

## 0.1.5

- 通过 WinEvent 前台窗口、最小化和位置变化事件即时同步全屏切入/切出；五秒轮询仅作为异常兜底。
- 任务栏层级发生变化时立即定位，并在 80ms 后复核一次，减少 DWM/Explorer 过渡阶段的残留延迟。
- 浮岛改为任务栏左侧对齐；系统 Widgets 可见时自动排列在 Widgets 按钮之后，避免内容重叠。
- Provider 缩写替换为与 Android 版共用路径的九家 Provider 矢量图标，高 DPI 下保持清晰。
- 浮岛由 `250×44 DIP` 缩小为 `225×38 DIP`，图标和两行文字同步缩小。
- 版本号和构建产物名称更新为 `0.1.5`。

## 0.1.4

- Windows 11 恢复为单一透明悬浮模式，不再提供任务栏嵌入或 Widgets 覆盖选项；旧设置会自动迁移为悬浮窗。
- 浮岛改为透明、无背景、无外框的双行布局：左侧 Provider 图标，右上 Provider/账户备注，右下余额与用量。
- 文字统一为白色并添加轻量阴影，改善浅色任务栏上的可读性。
- 悬浮窗口使用任务栏显示器的实际 DPI 计算物理尺寸和位置，并在 DPI、任务栏状态变化后自动重新定位。
- 全屏应用遮挡任务栏时将悬浮窗移到底层，任务栏恢复后重新显示。
- Windows 10 保留任务栏兼容模式代码和切换入口，等待后续实机验证。
- 版本号和构建产物名称更新为 `0.1.4`。

## 0.1.3

- 新增“Widgets 按钮覆盖”模式：读取 Windows 11 原生 Widgets 按钮矩形并原位显示余额与今日用量。
- Widgets 按钮不可定位时自动回退到现有任务栏兼容模式，不进行 Explorer DLL 注入。
- 兼容窗口与 Widgets 覆盖层现在跟随任务栏可见性调整 Z 序；全屏应用遮挡任务栏时不再悬浮到应用之上。
- UI Automation 任务栏结构读取增加五秒缓存，日常一秒巡检只执行轻量窗口命中检测。
- 版本号和构建产物名称更新为 `0.1.3`。

## 0.1.2

- 修复 Windows 11 选择任务栏嵌入后浮岛完全消失的问题。
- 参考 TrafficMonitor：Explorer 拒绝跨进程嵌入时自动切换到通知区域/任务栏左侧的置顶伴随窗口，不再隐藏挂件或只显示错误。
- 任务栏位置未变化时不再重复触发窗口框架刷新、强制重绘和 DWM 刷新，消除兼容模式的周期性闪烁。
- Windows 11 改用 popup-parented layered surface，让 WPF 浮岛保留独立 DWM 合成层；Windows 10 继续使用 child host。
- 嵌入后强制刷新 DWM，并检测不可见或 cloaked 状态；检测失败时立即恢复悬浮模式并显示原因。

## 0.1.1

- 设置窗口现在跟随 Windows 应用深浅色模式，并在系统主题切换后实时更新。
- 为按钮、文本框、密码框、Provider 下拉框、下拉菜单、表格表头、行、单元格、选中态和禁用态补齐深色资源。
- Windows 10/11 原生标题栏同步启用或关闭 DWM 深色模式；任务栏浮岛背景与文字也随应用主题更新。
- 新增可持久化的“悬浮窗 / 任务栏嵌入（实验）”显示模式切换。
- 嵌入模式在 Windows 11 居中任务栏使用左侧空位；左对齐任务栏和 Windows 10 使用通知区域左侧空位。
- Explorer 重启、任务栏重建、DPI 或对齐变化后自动重新挂载；没有安全空间时回退悬浮模式。

## 0.1.0 MVP

- 以 Android `v0.9.2` 的 Provider、刷新和显示行为为基线建立 Windows 工程。
- 新增托盘常驻和任务栏内余额文字条，每 5 秒轮换、单击立即切换。
- 新增九个 Provider 的余额查询或官方 Key 验证。
- 新增 Windows Credential Manager API Key 存储和本地 JSON 状态。
- 新增每账户刷新周期、30 秒手动防抖、429 `Retry-After` 与最长 24 小时退避。
- 新增手动余额、分级状态、余额通知基础设施和本地今日用量估算。
- 新增 Windows GitHub Actions 构建与可下载的 `win-x64` artifact。
